# 🚀 Cloudflare Pages 快速部署指南

## 第一步：提交代码到 Git

```bash
# 1. 添加所有文件
git add .

# 2. 提交更改
git commit -m "配置 Cloudflare Pages 部署"

# 3. 如果还没有远程仓库，先创建 GitHub 仓库，然后：
git remote add origin https://github.com/你的用户名/my-site.git
git branch -M main
git push -u origin main
```

## 第二步：在 Cloudflare Pages 创建项目

### 1. 进入 Cloudflare Dashboard
- 访问：https://dash.cloudflare.com/
- 登录你的账号（Xiaogangfan@126.com）

### 2. 创建 Pages 项目
- 在左侧菜单找到 **"Workers & Pages"**（或直接搜索 "Pages"）
- 点击 **"Create application"**
- 选择 **"Pages"** 标签
- 点击 **"Connect to Git"**

### 3. 连接 Git 仓库
- 选择你的 Git 提供商（GitHub / GitLab / Bitbucket）
- 首次使用需要授权 Cloudflare 访问你的仓库
- 授权后，选择仓库：`my-site`

### 4. 配置构建设置

**重要配置项：**

| 配置项 | 值 |
|--------|-----|
| **Project name** | `my-site`（或自定义名称） |
| **Production branch** | `main` |
| **Framework preset** | `Next.js`（可选，会自动检测） |
| **Build command** | `npm run build` |
| **Build output directory** | `out` ⚠️ 重要！ |
| **Root directory** | `/`（保持默认） |

### 5. 环境变量（可选）
- 如果项目不需要环境变量，可以跳过
- 需要的话在 **"Environment variables"** 中添加

### 6. 开始部署
- 点击 **"Save and Deploy"**
- 等待构建完成（通常 2-5 分钟）

## 第三步：获取免费域名

部署成功后，你会看到：

✅ **部署成功页面**
- 显示你的网站 URL：`https://my-site.pages.dev`（或你自定义的名称）
- 这个域名**国内可以直接访问**！

## 第四步：验证部署

1. **访问网站**
   - 打开部署后的 URL
   - 检查页面是否正常显示

2. **测试自动部署**
   - 修改 `app/page.tsx` 中的内容
   - 提交并推送到 `main` 分支
   - Cloudflare Pages 会自动重新部署

## 🎯 常见问题

### Q: 构建失败怎么办？
A: 检查以下几点：
- ✅ 构建命令是否正确：`npm run build`
- ✅ 输出目录是否正确：`out`（不是 `.next` 或 `dist`）
- ✅ 查看构建日志中的错误信息

### Q: 如何绑定自定义域名？
A: 
1. 在 Pages 项目页面，点击 **"Custom domains"**
2. 点击 **"Set up a custom domain"**
3. 输入你的域名
4. 按照提示配置 DNS（如果域名在 Cloudflare，会自动配置）

### Q: 如何查看访问统计？
A:
- 在 Pages 项目页面，点击 **"Analytics"** 标签
- 可以查看访问量、带宽使用等数据

## 📝 部署后检查清单

- [ ] 代码已推送到 Git 仓库
- [ ] Cloudflare Pages 项目已创建
- [ ] 构建命令设置为：`npm run build`
- [ ] 输出目录设置为：`out`
- [ ] 部署成功，网站可以访问
- [ ] 测试自动部署功能

## 🎉 完成！

现在你的网站已经部署在 Cloudflare Pages 上了！

- **免费域名**：`https://my-site.pages.dev`
- **国内可访问**：✅
- **自动部署**：每次 Git push 自动更新
- **完全免费**：✅

---

**提示**：如果需要更多帮助，查看 [DEPLOY.md](./DEPLOY.md) 获取详细文档。

